{
  "name": "canopy-website-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo-form ingest shim for canopy-website. Public HTTPS endpoint (Tailscale Funnel) that writes to in-cluster Postgres.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "postgres": "^3.4.8",
    "zod": "^3.24.1"
  }
}
